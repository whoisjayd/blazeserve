apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazeserve
  namespace: default
  labels:
    app.kubernetes.io/name: blazeserve
    app.kubernetes.io/part-of: edge-infrastructure
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: blazeserve
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blazeserve
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: blazeserve
          image: ghcr.io/whoisjayd/blazeserve:latest
          imagePullPolicy: IfNotPresent
          args: ["serve", "/data", "--host", "0.0.0.0", "--port", "8000", "--cors", "--log-json"]
          ports:
            - containerPort: 8000
              name: http
          livenessProbe:
            httpGet:
              path: /__live__
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /__ready__
              port: 8000
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          emptyDir: {}
        - name: tmp
          emptyDir: {}
