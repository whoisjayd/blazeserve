# High-performance Nginx Reverse Proxy for BlazeServe
worker_processes auto;
events {
    worker_connections 65535;
    multi_accept on;
    use epoll;
}

http {
    include mime.types;
    default_type application/octet-stream;

    # Kernel optimizations
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;

    upstream blazeserve_backend {
        server 127.0.0.1:8000;
        keepalive 64;
    }

    server {
        listen 80;
        server_name _;

        # Disable buffering for low-latency streaming and huge range requests
        proxy_buffering off;
        proxy_request_buffering off;
        proxy_http_version 1.1;

        # Unlimited body size for streaming file uploads
        client_max_body_size 0;

        location / {
            proxy_pass http://blazeserve_backend;
            proxy_set_header Connection "";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Request-ID $request_id;

            # Extended read timeouts for multi-gigabyte transfers
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Operational probe passthrough without access logging noise
        location ~ ^/(__live__|__ready__|__health__|__metrics__|metrics) {
            proxy_pass http://blazeserve_backend;
            access_log off;
        }
    }
}
